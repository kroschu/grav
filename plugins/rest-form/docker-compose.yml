version: '3'

services:
  app:
    build: ./docker/app
    image: restform:latest
    container_name: demo
    ports:
      - 80:80
      - 22:22
    #volumes:
    #  - .:/usr/share/nginx/html/user/plugins/rest-form